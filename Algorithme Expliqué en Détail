Algorithme TableauUnique
Début
    // 🎯 PROGRAMME PRINCIPAL
    saisir(N)        // Appel de la procédure de saisie de N
    remplir(t,n)     // Appel de la procédure de remplissage du tableau
    affichage(t,n)   // Appel de la procédure d'affichage du tableau
fin

┌───────────────────────────────────────────────────────────┐
│                      Nouveaux Types                       │
├───────────────────────────────────────────────────────────┤                     
│               Objet              │      Nature / Type     │                               
├──────────────────────────────────┼────────────────────────┤                          
│               TDOG               │                        │
├──────────────────────────────────┼────────────────────────┤
│ n                                │ entier                 │
│ t                                │ tab                    │
│ saisir,remplir,affichage         │ procedure              │
└──────────────────────────────────┴────────────────────────┘

// 📥 PROCÉDURE DE SAISIE DE LA TAILLE DU TABLEAU
procédure saisir(@x:entier)
début 
    Répéter
        Écrire("Entrez x entre5-10 : ")  // Message de saisie
        Lire(x)                          // Lecture de la valeur
    Jusqu'à (n ≥ 5) ET (n ≤ 10)         // Condition de validation (5 ≤ n ≤ 10)
fin

// 🎯 PROCÉDURE DE REMPLISSAGE DU TABLEAU
procédure remplir(@t:tab,n:entier)
debut
    Pour i de 0 à n-1 Faire              // Boucle de remplissage
        Répéter
            Écrire("T[", i, "] = ")      // Affichage de l'index
            Lire(T[i])                   // Lecture de la valeur
        jusqu'à t[i]>0 et unique(t,n)    // Condition : positif ET unique
    finpour
fin

// 📋 TDOL POUR REMPLIR
┌──────────────────────────────────┬────────────────────────┐
│               Objet              │      Nature / Type     │
├──────────────────────────────────┼────────────────────────┤
│ i                                │ entier                 │
└──────────────────────────────────┴────────────────────────┘

// 🔍 FONCTION DE VÉRIFICATION D'UNICITÉ
fonction unique(t:tab,n:entier) : booléen
début
    i=0           // Initialisation de l'index
    test=vrai     // Initialisation du test à vrai
    répéter
        si nb-occurance(t,n,i>1) alors  // Si nombre d'occurrences > 1
            i=i+1                       // Incrémentation de l'index
        sinon 
            test=faux                   // Le test devient faux
        finsi   
    jusqu'à  test=faux ou i≤ n         // Condition d'arrêt
    retourner test                      // Retour du résultat
fin

// 📋 TDOL POUR UNIQUE
┌──────────────────────────────────┬────────────────────────┐
│               Objet              │      Nature / Type     │
├──────────────────────────────────┼────────────────────────┤
│ i                                │ entier                 │
│ test                             │ booléen                │
│ nb-occurance                     │ fonction               │
└──────────────────────────────────┴────────────────────────┘

// 🔢 FONCTION DE COMPTAGE D'OCCURRENCES
fonction nb-occurance(t:tab,n,x:entier)
début                                                 
    nb<---0                           // Initialisation du compteur
    pour i de 0 à n-1 faire           // Parcours du tableau
         si t[i]=x alors              // Si valeur trouvée
            nb=nb+1                   // Incrémentation du compteur
         finsi                                                                                   
    finpour
    retourner nb                       
fin

// 📋 TDOL POUR NB-OCCURANCE
┌──────────────────────────────────┬────────────────────────┐
│               Objet              │      Nature / Type     │
├──────────────────────────────────┼────────────────────────┤
│ x                                │ entier                 │
│ i                                │ entier                 │
│ nb                               │ entier                 │
└──────────────────────────────────┴────────────────────────┘

// 📊 PROCÉDURE D'AFFICHAGE DU TABLEAU
procédure affichage(t,n)
    Écrire("Tableau T : ")            // Message d'en-tête
    Pour i de 0 à n-1 Faire           // Parcours du tableau
        Écrire(T[i], " ")             // Affichage de chaque élément
    FinPour
Fin

// 📋 TDOL POUR AFFICHAGE
┌──────────────────────────────────┬────────────────────────┐
│               Objet              │      Nature / Type     │
├──────────────────────────────────┼────────────────────────┤
│ i                                │ entier                 │
└──────────────────────────────────┴────────────────────────┘
